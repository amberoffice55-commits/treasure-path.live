<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>TreasurePath — Investment Platform (demo)</title>

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">

  <!-- Basic SEO -->
  <meta name="description" content="TreasurePath — Demo investment platform UI (static clone).">

  <style>
    :root{
      --bg:#070617;
      --accent1:#ffb957;
      --accent2:#6be7f8;
      --muted:#9aa4b2;
      --card:#0f1724;
      --glass: rgba(255,255,255,0.03);
      --glass-2: rgba(255,255,255,0.02);
      --success: #18c985;
      --danger: #ff6b6b;
      --radius: 14px;
      font-family: 'Inter',system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial;
    }

    *{box-sizing:border-box}
    html,body{height:100%}
    body{
      margin:0;
      background:
        radial-gradient(800px 500px at 10% 10%, rgba(107,231,248,0.06), transparent 8%),
        radial-gradient(700px 400px at 95% 90%, rgba(255,185,87,0.05), transparent 8%),
        linear-gradient(180deg,#05050b 0%, #071023 100%);
      color:#e6eef6;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.45;
      padding-bottom:60px;
    }

    /* Container */
    .wrap{
      max-width:1100px;
      margin:0 auto;
      padding:36px 20px;
    }

    /* NAV */
    header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:16px;
      margin-bottom:28px;
    }
    .brand{
      display:flex;
      gap:12px;
      align-items:center;
      text-decoration:none;
      color:inherit;
    }
    .logo{
      width:52px;height:52px;border-radius:12px;
      background:linear-gradient(135deg,var(--accent1),var(--accent2));
      display:flex;align-items:center;justify-content:center;
      box-shadow:0 6px 18px rgba(0,0,0,0.45), inset 0 -6px 18px rgba(255,255,255,0.06);
      font-weight:800;color:#08121a;font-size:18px;
    }
    nav ul{display:flex;gap:18px;list-style:none;padding:0;margin:0;align-items:center}
    nav a{color:var(--muted);text-decoration:none;font-weight:600}
    nav a:hover{color:var(--accent2)}
    .cta{
      background: linear-gradient(90deg,var(--accent1),var(--accent2));
      color:#06121a;padding:10px 14px;border-radius:10px;font-weight:700;
      box-shadow: 0 8px 24px rgba(107,231,248,0.06);
      text-decoration:none;
    }

    /* HERO */
    .hero{
      display:grid;
      grid-template-columns: 1fr 420px;
      gap:28px;
      align-items:center;
      margin-bottom:34px;
    }
    .hero-card{
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius: var(--radius);
      padding:28px;
      box-shadow: 0 6px 30px rgba(2,6,23,0.6);
      border: 1px solid rgba(255,255,255,0.03);
      position:relative;
      overflow:hidden;
    }
    .super{
      color:var(--accent2);
      font-weight:800;
      font-size:13px;
      display:inline-block;
      background: rgba(107,231,248,0.06);
      padding:6px 10px;border-radius:999px;margin-bottom:12px;
    }
    h1{font-size:28px;margin:6px 0 12px;line-height:1.08}
    p.lead{color:var(--muted);margin:0 0 18px}

    .hero-actions{
      display:flex;gap:10px;margin-top:10px;
    }
    .btn{padding:12px 16px;border-radius:10px;font-weight:700;border:0;cursor:pointer}
    .btn-outline{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted)}
    .btn-primary{background:linear-gradient(90deg,var(--accent1),var(--accent2));color:#08121a}

    /* right column card */
    .card-stats{padding:18px;border-radius:12px;background:linear-gradient(180deg, rgba(255,255,255,0.015), transparent)}
    .stat-row{display:flex;justify-content:space-between;gap:10px;margin-top:10px}
    .stat{
      background:var(--glass);
      padding:12px;border-radius:10px;flex:1;text-align:center
    }
    .stat h3{margin:0;font-size:18px;color:var(--accent2)}
    .stat p{color:var(--muted);margin:4px 0 0;font-weight:600}

    /* PACKAGES / CARDS */
    .grid{
      display:grid;
      grid-template-columns: repeat(auto-fit,minmax(220px,1fr));
      gap:16px;margin-top:22px;
    }
    .plan{
      background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.02));
      border-radius:12px;padding:18px;border:1px solid rgba(255,255,255,0.03);
      transition:transform .25s ease, box-shadow .25s ease;
    }
    .plan:hover{transform:translateY(-8px);box-shadow:0 18px 40px rgba(2,6,23,0.55)}
    .plan .title{font-weight:800;margin-bottom:6px}
    .plan .price{font-size:20px;color:var(--accent1);font-weight:800}
    .plan ul{padding-left:18px;color:var(--muted);margin:10px 0 0}
    .plan .join{margin-top:12px;display:block;text-decoration:none;background:linear-gradient(90deg,var(--accent1),var(--accent2));color:#06121a;text-align:center;padding:10px;border-radius:10px;font-weight:800}

    /* FAQ & contact */
    .two-col{display:grid;grid-template-columns:1fr 360px;gap:20px;margin-top:26px}
    .faq .item{background:var(--glass-2);padding:14px;border-radius:10px;margin-bottom:10px;cursor:pointer;border:1px solid rgba(255,255,255,0.02)}
    .faq .q{display:flex;justify-content:space-between;align-items:center}
    .faq .a{margin-top:8px;color:var(--muted);display:none}
    .faq .open .a{display:block}

    .contact{
      background:linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.0));
      padding:18px;border-radius:12px;border:1px solid rgba(255,255,255,0.03)
    }
    .input{width:100%;padding:10px;border-radius:8px;background:rgba(255,255,255,0.02);border:1px solid rgba(255,255,255,0.03);color:inherit}
    .form-row{display:flex;gap:10px}
    .form-row .input{flex:1}

    /* FOOTER */
    footer{margin-top:36px;text-align:center;color:var(--muted);font-size:13px;padding:26px 20px}
    .small{font-size:12px;color:var(--muted)}

    /* Responsive */
    @media (max-width:980px){
      .hero{grid-template-columns: 1fr}
      .two-col{grid-template-columns:1fr}
      nav ul{display:none}
    }
  </style>
</head>
<body>
  <div class="wrap">

    <header>
      <a class="brand" href="#">
        <div class="logo">TP</div>
        <div>
          <div style="font-weight:800">TreasurePath</div>
          <div style="font-size:12px;color:var(--muted)">Digital asset investment</div>
        </div>
      </a>

      <nav aria-label="Main navigation">
        <ul>
          <li><a href="#home">Home</a></li>
          <li><a href="#packages">Packages</a></li>
          <li><a href="#faq">FAQ</a></li>
          <li><a href="#contact">Contact</a></li>
          <li><a class="cta" href="#signup">Sign up</a></li>
        </ul>
      </nav>
    </header>

    <!-- HERO -->
    <section id="home" class="hero">
      <div class="hero-card" role="region" aria-label="Introduction">
        <span class="super">SECURE • INSTITUTIONAL • 24/7</span>
        <h1>Grow your digital assets with intelligent portfolios and instant access</h1>
        <p class="lead">A demo front-end inspired by the TreasurePath site: deposit Bitcoin, Ethereum & USDT and access diversified strategies. This static demo reproduces layout & components for design and dev use. </p>

        <div style="display:flex;gap:12px;align-items:center;margin-top:8px">
          <button class="btn btn-primary" onclick="openSignup()">Get started</button>
          <button class="btn btn-outline" onclick="openAbout()">Learn more</button>
        </div>

        <div style="margin-top:18px;display:flex;gap:10px;align-items:center">
          <div style="display:flex;gap:8px;align-items:center">
            <img src="https://images.unsplash.com/photo-1605792657660-1fa2d4f3f8ce?q=80&w=96&auto=format&fit=crop&crop=faces" alt="" style="width:48px;height:48px;border-radius:10px;object-fit:cover;border:1px solid rgba(255,255,255,0.03)">
            <div style="font-size:13px">
              <div style="font-weight:700">Albert Patterson</div>
              <div style="color:var(--muted);font-size:12px">Founder & CEO</div>
            </div>
          </div>

          <div style="margin-left:auto;color:var(--muted);font-size:13px">Trusted by investors worldwide</div>
        </div>

        <!-- decorative -->
        <svg style="position:absolute;right:-80px;top:-40px;opacity:0.06" width="280" height="200" viewBox="0 0 600 400" xmlns="http://www.w3.org/2000/svg">
          <defs>
            <linearGradient id="g" x1="0" x2="1">
              <stop offset="0%" stop-color="#ffb957"/>
              <stop offset="100%" stop-color="#6be7f8"/>
            </linearGradient>
          </defs>
          <path d="M0 300 Q150 20 600 120 L600 400 L0 400 Z" fill="url(#g)"/>
        </svg>
      </div>

      <!-- right column -->
      <aside class="card-stats" aria-hidden="false">
        <div style="display:flex;justify-content:space-between;align-items:center">
          <div style="font-weight:800">Account overview</div>
          <div style="color:var(--muted);font-size:13px">Live (demo)</div>
        </div>

        <div class="stat-row">
          <div class="stat">
            <div style="font-size:12px;color:var(--muted)">Active Days</div>
            <h3 id="counter-days">0</h3>
            <p>Days</p>
          </div>
          <div class="stat">
            <div style="font-size:12px;color:var(--muted)">Active Users</div>
            <h3 id="counter-users">0</h3>
            <p>Users</p>
          </div>
        </div>

        <div class="stat-row">
          <div class="stat">
            <div style="font-size:12px;color:var(--muted)">Total Deposits</div>
            <h3 id="counter-deposits">$0</h3>
            <p>Deposits</p>
          </div>
          <div class="stat">
            <div style="font-size:12px;color:var(--muted)">Total Withdrawals</div>
            <h3 id="counter-withdraw">$0</h3>
            <p>Withdrawn</p>
          </div>
        </div>

        <div style="margin-top:12px">
          <a class="btn btn-outline" href="#packages" style="width:100%;display:inline-block;text-align:center">View Packages</a>
        </div>
      </aside>
    </section>

    <!-- PACKAGES -->
    <section id="packages">
      <div style="display:flex;justify-content:space-between;align-items:end;gap:12px">
        <div>
          <h2 style="margin:0 0 6px">Investment Packages</h2>
          <div class="small">Choose a plan that suits your appetite and timeline</div>
        </div>
        <div style="color:var(--muted);font-size:13px">Payments: BTC • ETH • USDT</div>
      </div>

      <div class="grid" style="margin-top:14px">
        <div class="plan">
          <div class="title">Starter</div>
          <div class="price">25% — 45% / 24h</div>
          <div style="color:var(--muted);font-size:13px">Min deposit $25 • Term: 24h</div>
          <ul>
            <li>Daily automatic payout</li>
            <li>Withdraw anytime after 24h</li>
            <li>24/7 support</li>
          </ul>
          <a class="join" href="#signup" onclick="openSignup()">Invest now</a>
        </div>

        <div class="plan">
          <div class="title">Advanced</div>
          <div class="price">75% — 120% / 7 days</div>
          <div style="color:var(--muted);font-size:13px">Min deposit $250 • Term: 7 days</div>
          <ul>
            <li>Higher returns</li>
            <li>Portfolio monitoring</li>
            <li>Priority support</li>
          </ul>
          <a class="join" href="#signup" onclick="openSignup()">Invest now</a>
        </div>

        <div class="plan">
          <div class="title">Institutional</div>
          <div class="price">Custom</div>
          <div style="color:var(--muted);font-size:13px">Contact us for tailored terms</div>
          <ul>
            <li>Asset management</li>
            <li>Account manager</li>
            <li>Compliance & reporting</li>
          </ul>
          <a class="join" href="#contact" onclick="scrollToContact()">Contact sales</a>
        </div>
      </div>
    </section>

    <!-- FAQ + Contact -->
    <section style="margin-top:26px">
      <div class="two-col">
        <div>
          <h3 id="faq">Frequently asked questions</h3>
          <div class="faq" style="margin-top:12px">
            <div class="item" data-open>
              <div class="q">
                <strong>What payment methods are accepted?</strong>
                <span style="color:var(--muted);font-size:12px">BTC • ETH • USDT</span>
              </div>
              <div class="a">We accept on-chain Bitcoin, Ethereum and USDT (ERC20/TRC20) deposits. This demo does not process real payments.</div>
            </div>

            <div class="item">
              <div class="q">
                <strong>Minimum deposit</strong>
                <span style="color:var(--muted);font-size:12px">$25</span>
              </div>
              <div class="a">Minimum deposit amount is $25 equivalent in crypto.</div>
            </div>

            <div class="item">
              <div class="q">
                <strong>How long before I can withdraw?</strong>
                <span style="color:var(--muted);font-size:12px">24 hours</span>
              </div>
              <div class="a">Withdrawals are permitted after the initial 24-hour period; some packages have specific terms.</div>
            </div>
          </div>
        </div>

        <aside id="contact" class="contact" aria-label="Contact form">
          <h4 style="margin-top:0">Contact support</h4>
          <p class="small" style="margin:6px 0 12px;color:var(--muted)">For demo purposes this form does not send real emails. Replace with your backend endpoint.</p>

          <div>
            <label class="small">Your name</label>
            <input class="input" id="name" placeholder="John Doe" />
          </div>
          <div style="margin-top:10px">
            <label class="small">Email</label>
            <input class="input" id="email" placeholder="email@example.com" />
          </div>
          <div style="margin-top:10px">
            <label class="small">Message</label>
            <textarea class="input" id="message" rows="4" placeholder="How can we help?"></textarea>
          </div>

          <div style="margin-top:12px;display:flex;gap:8px">
            <button class="btn btn-primary" onclick="sendContact()">Send message</button>
            <button class="btn btn-outline" onclick="resetContact()">Reset</button>
          </div>

          <div id="contact-status" style="margin-top:10px;font-size:13px;color:var(--muted)"></div>
        </aside>
      </div>
    </section>

    <!-- FOOTER -->
    <footer>
      <div style="margin-bottom:8px">© <span id="year"></span> TreasurePath (demo). All rights reserved.</div>
      <div class="small">Address: 16 Bishopthorpe Road, Pembury, TN2 2GS, UK • Email: admin@treasurepath.live</div>
    </footer>

  </div>

  <script>
    // Small helper utilities and interactive behaviors
    document.getElementById('year').textContent = new Date().getFullYear();

    // Animated counters (digit-by-digit accurate)
    // We purposely compute digits step by step
    function animateCounter(id, target, prefix='', duration=1500) {
      const el = document.getElementById(id);
      if (!el) return;
      const start = 0;
      const startTime = performance.now();
      function tick(now) {
        const t = Math.min(1, (now - startTime) / duration);
        const ease = t < 0.5 ? 2*t*t : -1 + (4 - 2*t)*t; // smooth ease
        const value = Math.floor(start + (target - start) * ease);
        el.textContent = prefix + value.toLocaleString();
        if (t < 1) requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    // Use numbers inspired by the live site (demo values)
    animateCounter('counter-days', 230);
    animateCounter('counter-users', 4821);
    // deposits and withdraws with dollar sign
    (function(){
      const deposits = 142954;
      const withdraw = 51902;
      // to format with $ and commas, supply prefix
      const elDep = document.getElementById('counter-deposits');
      const elW = document.getElementById('counter-withdraw');
      // specialized animator that includes comma formatting for dollars
      function moneyAnim(el, target, duration){
        const startTime = performance.now();
        const start=0;
        function tick(now){
          const t = Math.min(1, (now - startTime)/duration);
          const value = Math.floor(start + (target-start)*(t));
          el.textContent = '$' + value.toLocaleString();
          if (t < 1) requestAnimationFrame(tick);
        }
        requestAnimationFrame(tick);
      }
      moneyAnim(elDep, deposits, 1400);
      moneyAnim(elW, withdraw, 1400);
    })();

    // simple FAQ toggle
    document.querySelectorAll('.faq .item').forEach(item=>{
      item.addEventListener('click', ()=>{
        item.classList.toggle('open');
      });
    });

    // Contact form (demo - no backend). Validate fields, show status.
    function sendContact(){
      const name = document.getElementById('name').value.trim();
      const email = document.getElementById('email').value.trim();
      const message = document.getElementById('message').value.trim();
      const status = document.getElementById('contact-status');

      function validEmail(e){
        // conservative email regex
        return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(e);
      }

      if (!name || !email || !message){
        status.style.color = 'var(--danger)';
        status.textContent = 'Please complete all fields.';
        return;
      }
      if(!validEmail(email)){
        status.style.color = 'var(--danger)';
        status.textContent = 'Please provide a valid email address.';
        return;
      }

      // Demo success - show simulated "sent" state
      status.style.color = 'var(--success)';
      status.textContent = 'Message queued — demo only (no real email sent).';

      // Clear form after a pause (simulated)
      setTimeout(()=> {
        document.getElementById('name').value='';
        document.getElementById('email').value='';
        document.getElementById('message').value='';
      },900);
    }

    function resetContact(){
      document.getElementById('contact-status').textContent = '';
      document.getElementById('name').value='';
      document.getElementById('email').value='';
      document.getElementById('message').value='';
    }

    // Small functions used by buttons
    function openSignup(){
      window.location.hash = '#signup';
      // If you'd wire to a modal / auth flow, do it here.
      alert('Signup flow — demo only. Replace with your sign-up page or modal.');
    }
    function openAbout(){
      window.location.hash = '#packages';
      // show more details, demo only.
    }
    function scrollToContact(){
      document.querySelector('#contact').scrollIntoView({behavior:'smooth'});
    }

    // Accessibility: keyboard support for FAQ
    document.querySelectorAll('.faq .item').forEach((it)=>{
      it.setAttribute('tabindex','0');
      it.addEventListener('keydown', (e)=> {
        if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); it.click(); }
      });
    });

    // small progressive enhancement: reveal hero after load
    window.addEventListener('load', ()=> {
      document.querySelectorAll('.hero-card, .card-stats, .plan').forEach((el,i)=>{
        el.style.opacity = 0;
        el.style.transform = 'translateY(8px)';
        setTimeout(()=> {
          el.style.transition = 'opacity .5s ease, transform .5s ease';
          el.style.opacity = 1;
          el.style.transform = 'translateY(0)';
        }, 120 + i*80);
      });
    });

  </script>
</body>
</html>
